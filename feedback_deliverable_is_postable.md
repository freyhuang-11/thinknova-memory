---
name: feedback-deliverable-is-postable
description: 交付给老板的文件必须是能直接照着发的,旁边不许躺旧版本、不许混我自己的分析
metadata:
  type: feedback
---

# 交付件 = 能直接照着发的东西(2026-08-16 老板连打两次)

老板原话:
1. 「**小红书新的文案怎么不是 txt 的,然后为什么没有覆盖掉老的内容,不然我怎么知道发哪个**」
2. 「**你第三篇的文案还都是格子啊**」

**Why**:老板打开文件夹的那一刻要能直接发。他不该在四个 `.bak` 里猜哪个是最新的,
也不该在我的自查注和复盘分析里找正文。**我一"留着以防万一",他就得替我做一次筛选。**

**How to apply**:交付目录里每篇只许有三样 —— 图 / `文案.txt`(要发的) / `_说明.md`(不发的)。

| 病 | 治法 |
|---|---|
| `.bak` `.bak2` `.bak3` 躺在正文旁边 | **移进** `_历史版本\`(归档=移动不是删除),文件名带上是哪一篇 |
| 正文里混着【形态】【改稿】【发布前】【要你知道的】 | 全搬进同目录 `_说明.md`(下划线开头,排在列表最后不挡路) |
| 段标题挂着「(12 字,上限 20)」这类自查注 | 砍掉,并进 `_说明.md` |
| 无 BOM 的 UTF-8 | **一律 `utf-8-sig`**,记事本双击不乱码(抖音那批就是这么存的) |
| 🔴 emoji 键帽 `1️⃣`–`5️⃣` | **记事本显示成方框**。它是三码位组合(数字 + U+FE0F + U+20E3)。<br>换成 **①②③④⑤**(U+2460–2464),中文字体都有,小红书也正常 |

**扫雷法**:交付前扫一遍非常规字符 —— 码位 ≥U+2000 且不在
汉字(4E00–9FFF)/中文标点(3000–303F)/全角(FF00–FFEF) 里的,逐个确认它在记事本里显不显得出来。
`🍲` 这类正文里特意放的 emoji 可以留(小红书上正常),但要**主动告诉老板它在记事本里是方框**,别让他自己发现。

相关:[[feedback_file_placement]] [[project_thinknova_xhs_line]]


## 🔴🔴 能自己判的就自己改，别退回去等拍板(08-24)

老板原话:「**你要改的地方你去改掉呀。不然我怎么拍，拍出来也没办法剪啊**」。

**病灶**:我把四处"名实不符/编造"全列成「要你拍板」退回去了——
可那四处**我自己看图就能判**(图上有没有红章、卡上印的是哪句话)，
不是他才知道的事。退回去的结果是**把他卡在开机之前**。

**判据(动手前问一句)**:
> 这件事的答案，是**在素材/文件里**，还是**只有老板脑子里**？
- 在素材里(图上有没有 X / 库里有没有 Y / 数字对不对) → **自己查、自己改、改完告诉他改了什么**
- 只有他知道(要不要花钱烧、这句是不是真事、方向对不对) → 才退回去问

**改完必须做的两件**:①旧说法**零残留复验**(grep 一遍) ②**同源的那份也改**
(桌面 TXT 改了，导演台完整版也要改，不留两份说法)。

## 🔴 字符串替换补丁必须加 assert(08-24 静默失败一次)
用 `s.replace(a,b)` 打补丁时**必须先 `assert s.count(a)==1`**。
08-24 我改去黑头脚本，模式没匹配上，`replace` **静默什么也没干**，
脚本压根没改却照跑了一遍，我还以为改好了。补上断言第二次才发现。

## 2026-09-06 · 日报/总结精简制(老板令:本机无效每日总结太多,统一归纳,做到精准简洁高效)
- 全线日报只保留**当日增量三行**(做了什么/数据变化/需老板动作),写进 vault `平台状态总览.md` 顶部,旧段落 7 天后归档;不再每线各写一份长日报。
- 每日输出目录里"没有事实增量"的文件(纯例程回执/重复快照)不产出;定时任务若当日无增量,写一行「无增量」而非整篇。
- 工作台/交付内容里同一主题的多版报告,收口时合并成一份"现行版",旧版移 99_归档 留台账。

## 2026-09-19 · 改一处对外口径 = 必须扫「所有指向它的地方」

**今天同一形状栽了三次**（三条线各一次）：
- 产品定义改了（砍一个档位）→ 下游四份文件没扫 → 三处对外话术还在用旧数
- 邮件模板口径改了（去掉秒数）→ **邮件指向的落地页没扫** → 收件人点进去照样看到旧口径 ⇒ **等于没改**
- 我自己：样片页正文从 5 条改成 3 条 → **摘要没跟着改** → 页面自己跟自己打架

🔴 **规矩（WHAT）**：改任何**对外**口径（价格 / 规格 / 承诺 / 数量），改之前先列一张清单：
1. **它本身**在哪几个文件里
2. **谁链到它**（邮件模板 → 落地页 → 页内 CTA）
3. **谁引用它**（话术包、作战包、制度文件、别的会话的稿子）
4. **谁正在用它**（有没有序列正在发、有没有排期正在跑）
⇒ 清单上每一项都扫过，才算改完。**只改文件本身 = 没改。**

🔴 **同一个文档内部也要扫**：正文改了，**摘要 / 标题 / SEO 描述 / 目录**都要跟着改。
   （PUT 是整体覆盖,我只换了正文字段就以为改完了。）

⚠️ **已经发出去的追不回** ⇒ 所以顺序是：**先停正在发的，再改静态的**，⛔ 不要反过来。

**DONE**：改完能说出「我扫了哪几处、各自现在写的是什么」。

关联：[[feedback-evidence-standard]] [[feedback-boss-rulings]]
