---
name: reference-hengping-x-pipeline
description: 横屏口播片走 X 式(抠像+白描边+示意图)的现状、规则出处、工程参数与踩过的坑
metadata:
  type: reference
---

# 横屏口播 · X 式管线(2026-08-16 打通)

## 🔴 规则散在五处,而且和现役代码是断的
老板 08-16:「横屏那个视频你之前给我做,**我说过了的那几单不也是嘛,我们后面定过一次规则啊**」
—— 规则确实定过,**定了五次**,1 份 md + 4 处代码/HTML 抬头注释:

| 时间 | 内容 | 出处 |
|---|---|---|
| 07-21 | **X 式 = 人像抠图 + 白描边 + 示意图**,「样片 X2 已验收通过」 | `99_归档\...\国内营销_脚本总库_旧线_归档0810.md:5` |
| 07-22 | 人像可遮挡背后素材,**素材做大不避让** | `_合成源文件\comp_W_base.html:9` |
| 07-20 夜 | 排版减法三条(无引导词/无页码/无引号图形) | `comp_full_V.html:9` |
| 08-11 | **《成片与封面·固定规则 v1》** 557 行,唯一成体系那份 | `00_规格与参考\成片与封面_固定规则_v1.md` |
| 08-13/15 | 横屏/竖屏分流 | `_管线\cut.py:3-22,258` |

⛔ **翻车根因**:`cut.py` 抬头写着「横屏的就小蓝那套」,**实现里一步抠像都没有**(906 行零 alpha 合成)。
07-21 的 X 式只躺在**已归档**的旧脚本总库里,没接进现役管线。照代码跑就跑偏。
→ **以后做横屏,先读 `_小蓝横屏配方_实挖_2026-08-16.md`(§⓪ 五次规则原文,带行号)。**

## 两条互相打架、老板还没裁的
1. **07-22「人压素材」vs 小蓝「图形绝不碰人」** —— `cut.py:321,333` 现在贴片碰到人会直接报错停机。
   ✅ **08-16 的绕法**:示意图全部锁在画面 **0–55%** 带内,人像头顶在 59%,两者天然碰不到,
   **不用逼老板现在选边**。
2. **抠像 vs 「横屏不许裁掉风景」(08-15)** —— 抠像就是把风景整个扔掉。08-16 老板选了抠像。

## 工程参数(实测,`_工程_横屏全片\`)
- 抠像 `npx hyperframes remove-background --quality best`(v0.7.109,u2net_human_seg)
- **抠像前整体降到 1440×810(等比 0.75),只缩不裁** —— 裁窄两次都切到右肩(右肩伸到源片 x≈1733)
- **20 帧/块**。峰值 RSS 由模型撑起(1.0–1.6GB),和块大小基本无关;块越大越容易把可用内存压到个位数,
  开始换页反而更慢(45帧 1.98s/帧 vs 20帧 1.30s/帧)
- 956 帧 = 48 块,零失败,单块 23–60s
- 🔴 **必须有的三件**(08-16 补的,不然一挂全白跑):
  ①`--resume` 断点续跑 ②每块开跑前**等内存回落**(等到 300MB 或最多 90s)
  ③`TIMEOUT` 从 600 降到 **240**(样片单块 35–66s;600 的代价是坏块死等 10 分钟×3 次,agent 就是这么被看门狗掐死的)
- 白描边:**照抄 `_工程_dy01-broll\v2_build.py:59-66` 的 lutrgb 版**。
  ⚠️ 正片①②的 `color=` 版不带 `d=` 是无限流,接进 `alphamerge` 永远不结束
- 人像居中按**头部中心**不按外接框中心(差 103px,按框渲人明显偏左);头顶目标 **59%**

## 本机内存是硬约束
8GB 总量,忙时可用只剩 100MB 上下;抠像单块峰值 1.3–1.5GB。**跑之前先看可用内存**,
`Get-CimInstance Win32_OperatingSystem` 的 `FreePhysicalMemory`。老板在用 WPS/浏览器时会更紧。

相关:[[project_thinknova_marketing]] [[feedback_kill_python_scope]]
