---
name: feedback-case-change-no-blast-radius
description: "触发:要为某一个案例/场景改配置之前 → 铁规:案例级问题只在案例层解;必须动全局时做成显式开关,不写暗号的案例行为逐字不变"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 5415ca52-b559-4c91-a28d-36c22f0d137f
  modified: 2026-08-07T15:23:24.257Z
---

老板 2026-08-07 立的硬约束（原话：「你要保证你切了这些内容其他的案例不会被影响啊」「你动一个案例的时候不要影响其他的内容啊」）。

## 铁规
**改一个案例，只许影响这一个案例。**

1. **案例级问题优先只在案例层（visualHint / scriptwriterPreset）解决**，不碰全局模板。
2. **确实是全局模板压制了案例（根因在全局）时**，不许直接放宽全局默认——必须做成**显式开关**：
   - 全局模板保留原句一字不改作为默认；
   - 追加一句窄口径条件，**以案例里的特定暗号为触发**（如「仅当案例写明『改造前后两态反差』时…」）；
   - 在目标案例的 visualHint 里埋入那句暗号。
   - 效果：689 个案例里没写暗号的 688 个，提示词逐字不变，**爆炸半径 = 0**。
3. **动全局前先量爆炸半径**：扫案例库统计有多少条会被新条件命中（`GET /admin/api/v1/agents/{code}/reference-cases?page=&pageSize=60` 分页拉全量，正则扫 visualHint）。命中数 > 1 就说明口径太宽，收窄重写。
4. 改完**必须烧一条对照单**（用与本次改动无关的常规案例，参数照抄一条已验收过的旧单），确认没被带偏，再报"不影响其他案例"。

## 血案（本条规则的来源）
08-07 修「前后对比」案例时，我第一版把全局模板的 `门店(同一空间同一光)全片锁死首帧` 直接放宽成 `光线与陈设默认不变,案例明确要求前后对比时按案例变化`。扫库发现 **689 个案例里 79 个 visualHint 含「前后/对比」字样**，其中「贵的和便宜的差在哪」这类产品对比案例本来就该保持同一光才公平——等于给 79 个案例开了口子。
改法：全局恢复原句，改成「仅当案例写明『改造前后两态反差』时…」，只有目标案例埋了暗号，命中数 = 1。

关联 [[project-thinknova-0729-screenwriter-stack]] [[feedback-scope-boundary-explicit]] [[feedback-directive-over-prohibition]]
