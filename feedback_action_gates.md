---
name: feedback-action-gates
description: "触发:每次要动手之前(改配置/改案例/烧单/全库扫描/铺开/下结论) → 五道闸门,每道要求先输出一个可检查的产物,不产出就不许往下走。这是所有规矩的调用入口。"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: 5415ca52-b559-4c91-a28d-36c22f0d137f
  modified: 2026-08-08T08:40:56.170Z
---

# 行动闸门（2026-08-08 老板令「你不能每次都再漏，对我们造成得损失是很大得」后重建）

## 为什么要有这个文件

我不缺规矩，我有 30 个 feedback 文件。**我缺的是在动手那一刻把规矩调出来。**
08-08 一天连犯 7 次，复盘下来是同一个动作模式：**看到现象 → 直接动手 → 出事 → 回滚**。
所以规矩不能再写成"要记得 X"——记得的东西会漏。**每道闸门都要求我先输出一个东西，输出不出来就是没过闸，不许往下走。** 产物是可检查的，记忆不是。

**通则：先读后动，先验后铺。**

---

## 闸 1 · 动手前（改任何东西 / 烧任何单 / 下任何结论）

**必须先输出一行**：
> 现状源：我读了 ＿＿＿，现状是 ＿＿＿

- 读的顺序：记忆文件 → 技术文档 → 线上 config/接口。三者冲突以**最新的线上真值**为准。
- 「只有/一定/不可能/全都」这类绝对判断，**一律回源核对后才许说**。
- 🔴 **烧单是验证手段，不是探索手段。** 现状从记忆和配置里读，不从烧单里猜。
- 08-08 代价：在案例层反复试声线（其实节奏归全局【口播节奏】段管）、在案例层改镜头运动（其实被全局"禁止镜头切换"压着）、架构分层记忆里本来就有我没调用。三条弯路全在同一天。

### 闸 1 附加条款 · 「查不到」不是结论，是我还没查完（08-08 老板当场抓出来）
我查了 3 个接口拿不到编剧报错，就下结论说「机制证不出来」，还写进了记忆。老板一句「你看一下之前 2 单出问题得编剧层得 10 次报错，我看是不一样得」——换个形式一调就全出来了（`ai-tasks/{task_no}` 通，我之前用数字 id 所以是 null）。**结论是错的，方向也被我带偏了。**

三条硬规：
1. **「取不到 / 没有接口 / 做不到」在说出口之前，必须先列出我试过的具体调用，并且至少换过一种形式**（id ↔ 编号、单数 ↔ 列表、商家端 ↔ admin、camelCase ↔ snake_case）。
2. **记忆里写着「这个接口返回 null / 不能用」，也要再试一次。** 那条记忆很可能是当初用错了形式记下的——我这次就是被自己写的错记忆挡住的。
3. **拿不到证据时，正确的话是「我还没拿到，我打算这样拿」，不是「证不出来，所以大概是 X」。** 用推测填证据的空位 = 制造下一个错误结论。今天我不但猜错，还把错的猜测写进了记忆，差点让后面所有改动都建在上面。

## 闸 2 · 改提示词（任何进 videoPrompt 的字段）

**必须先输出一张字节账**：
> 字段 ＿＿＿：现 N 字节 → 改后 M 字节，Δ=＿＿＿；挤掉的是 ＿＿＿

- **只减不增。** 必须加就先删等量。**Δ>0 且答不出"挤掉了谁" = 没过闸。**
- i2v videoPrompt **4096 字节是死规矩**（videoTemplate 939B + cells 2.4-3.0KB 已贴 3.3-3.9KB）。提示词总字数 ≤4000 字。
- 🔴 **案例 visualHint 里一个字都不许写声线/语调/语速/换气/停顿/气息**——归全局 videoTemplate【口播节奏】段管。碰了就是台词全空。
- 🔴 现存盲区：**没有任何接口能返回拼装后的完整 videoPrompt**（admin 列表截断 280 字、plan 只给摘要、ai-tasks/{id} 返回 null）。在拿到全文接口之前，加字改动 = 蒙眼飞行。已列待发技术卡。

## 闸 3 · 改案例（改一个案例只许影响这一个案例）

**必须先输出一个爆炸半径数**：
> 扫库命中 ＿＿＿ 条，目标 1 条

- 🔴 **先查有没有上层规则会把这条改动压掉**（08-08 第二次踩）。案例级要求打不过全局/案例内的硬规：
  - 前后对比案例要「光线变化」→ 被全局「同一空间同一光全片锁死」压死
  - 36 条人物讲述型要加情绪面部变量 → 被案例自己的分镜硬规「五格只允许台词和手势不同」压死
  **改之前先全文搜一遍冲突项，冲突就必须成对改，单改一边等于白改。**
- 命中 >1 就**收窄**，不许开口子。根因在全局模板时，做成**显式开关**：全局原句一字不动 + 窄口径条件（"仅当案例写明「＿＿」时"），暗号只埋进目标案例。
- 改完烧一条**无关案例**的对照单，证明没带偏。
- 实证：给"前后对比"放宽全局，扫库发现 79 条含"前后/对比"会被误命中；改成暗号开关后命中 = 1，对照单证明爆炸半径 0。

## 闸 4 · 验证（改完第一件事）

**必须先输出 attemptCount，再看成片**：
> `output.childTasks.scriptwriter.attemptCount` = ＿＿＿

| 值 | 含义 | 动作 |
|---|---|---|
| **1** | 正常 | 继续验成片 |
| **3-4** | 已在撞边界（台词被压短/变空） | 停，回滚 |
| **10** | **确认顶穿**，每次尝试都被杀 | 立即回滚 |

- 还要验三件：`task.model` 实际派发（认 `videoModelId`）、编剧 `source`（回退单/被劫持单一律作废重烧）、台词字数落窗。
- 🔴 **烧单必须轮换行业，不许盯着餐饮烧**（老板 08-08 明令）。同一批验证里行业要分散。
- 逐帧通看（ffmpeg 联系表）+ silencedetect 量死寂；单帧截图 = 假结论。
- 落库 ≠ 送达：PUT 200 只证明写进库，前台/烧单看见才算上线。

## 闸 5 · 铺开前（从 1 条到 N 条）

**必须先输出**：
> 单条验证：case ＿＿＿ / 任务号 ＿＿＿ / attemptCount ＿ / 结论 ＿＿＿ → 建议第一批 ＿＿ 条，因为 ＿＿＿

- **老板过目之后才许批量。** 风格类改动一律「单刀 → AB 烧验 → 老板过目 → 再下一刀」。
- **台词层一次只许动一条规则。** 实证：一次堆五条（短句+惊叹开头+写满上限+句数只多不少+开场兴奋）→ 台词情绪节奏三项全崩，老板评"最差的一单"。
- 有取舍/有截断/有抽样，**必须写出来**。默默砍掉范围再报"全部完成" = 事故。

---

## 附 · 全库扫描三条（08-08 体检翻车换来的）

1. **先统计样板出现率、剥离样板，再扫业务内容。** 不剥样板就上正则 → 我报出 507/597 这种数字，全是把标准模板当缺陷。
2. **先按 `enabled` 过滤。** 忘了这条，数字虚高一倍（173 其实是 83）。同理要剔除**合法例外**（S14 广告 TVC 本来就没台词段）。
3. **数字大到反常时先怀疑自己的规则，不要怀疑数据。** 误报比漏报更危险——漏报是少干活，误报是让老板以为全库崩了。
4. 🔴 **报数之前，随机抽 3 条人工读一遍原文，确认规则判对了才许报。** 08-08 我一天栽三次（507/597 误报 → 忘了 enabled → 情绪正则把「头」当面部变量，`镜头`两个字让 571/588 条全部误判成"有情绪"）。前两次犯完我把规矩写进了闸门，第三次照样犯——**说明光写进规矩不够，必须有这个动作**。抽检成本是 1 分钟，误报成本是老板按错的数字做决定。

## 附 · 并行

多任务并行时**开子 agent 跑**，不要串行自己干（老板 08-08：「我不是和你说过嘛，多任务并行得时候开多线程啊」）。
扫描类、调研类、逐条改写类都适合丢给子 agent；**写配置和烧单我自己来**，不交给 agent。

---

关联 [[feedback-prompt-change-hard-rules]] [[feedback-case-change-no-blast-radius]] [[feedback-source-truth-first-commander]] [[feedback-directive-over-prohibition]] [[feedback-evidence-standard]] [[feedback-context-budget-discipline]]
