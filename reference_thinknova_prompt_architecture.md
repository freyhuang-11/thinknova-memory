---
name: reference-thinknova-prompt-architecture
description: "触发:要改任何提示词之前 → 三层职责分工/海报视频分家/编剧现行规则/三条链上限各不同(编剧算字/生视频算字节/生图另一条),改后必量字节/grok 能力边界"
metadata: 
  node_type: memory
  type: reference
  originSessionId: 5415ca52-b559-4c91-a28d-36c22f0d137f
  modified: 2026-08-11T13:55:12.184Z
---
> 🔴 **本文里出现的一切字数/字节/条数都是当时的历史记录,不是现值。要现值一律现拉线上 config。**(2026-08-27 立)

> 只留**当前有效规则**;实验流水、证据单号、视觉纪律全文在 `archive/thinknova_arch_history_to_0729.md`;被推翻的旧结论已删(查 git)。
> 🔴 与技术官方文档冲突时**以技术文档为准**([[reference-thinknova-tech-docs-index]])。

## 一、两条铁律(07-09 拍定,违反即回炉)
1. **每段 prompt 自包含**:编剧/t2i/i2v 每次调用无状态,只拿到「本段文字+本次传入图」,看不到平台案例/配置/上一环产出。禁引用"后台配置""上文/之前编剧"。
2. **视频画面零字幕**(视频专属,**海报不受此限**):台词只念不画进画面;画进分镜图→裁首帧→进视频=严重降质。导演信息走专用标注带。

## 二、三层职责(仅视频线)
- **编剧**=演什么(每格画面+台词+灯光/情绪/摄影/音频标注),同场景每次尽量不同。
- **生图 t2i/i2i**=画得对且真(主体/白缝/负面/真实感灯光种子),**不写死画面、不画任何字、不放语气**。
- **i2v**=怎么动+念:四细节(画面按格推进/微距肤质/口播声线情绪/灯光延续首帧)+参考图三锁(人物·产品·场景,按上传条件注入)+零字幕+自包含。
- 核心:生图/i2v 只给护栏与风格,具体画面台词交编剧 → 既去冲突又保多样性。

### 字段路径(🔴 以线上 config 回读为准,schema 演进过)
> 🔴🔴 **权限红线(07-24)**:config 是前端/编剧的契约,**结构/字段/新增字段一律交技术,我只改纯文本字段**。[[feedback-dont-edit-prod-config-structure]]
- `promptComposer.screenwriter.systemPrompt` / `.staticTemplates.{firstFrameTemplate,videoTemplate}` — 编剧主体。**videoTemplate = i2v「视频总览」段真源头**,编译进每条 i2v。
- `promptComposer.masterPipeline.scriptwriter.*` — `lineValidation` / `timeline.defaultShotCount` / `fallbackPolicy` / `staticTemplates.contextPriority`。
- `stagePromptPresets.{image_to_video,text_to_image,image_to_image}` — i2v / 生图预设。
- 🔴🔴 **`opsEditable.*` 与 `blockTemplates.*` 镜像必须同时写**,只写一个 → PUT 200 但静默回滚(07-27 实测,破了长期"保存了没生效"悬案)。
  ❌「只维护 opsEditable、不要双写」已推翻 → 两边同写(07-27)。

## 三、海报 ≠ 视频(双向边界)
海报=静态营销图,**本来就要有文字**(标题/价格/CTA),铁律2 不适用;CTA 要正确文案不是枚举值;无六宫格/分镜/i2v/语气概念。**海报编剧无 config 侧 systemPrompt**,规则全在 `blockTemplates.copy_rules`(卖点角标只写顾客视角 2-6 字短语、**指引句必须转译不许照抄**;补充要求的风格/颜色/构图词**只作画面处理不上画面**)。**改视频不碰海报链路,改海报不套视频零字幕/分镜。**

## 四、编剧现行规则
- 🔴🔴 **长度唯一准绳 = `lineValidation`,语义=全片 lines 总计(不是每句!07-31 血案定案)**;⛔ 旧记的 zh{62,75} **已作废**;⛔「无时长维度」也已作废(现有 10s/15s 分档)。**具体窗口一律现拉线上。** 结构=flat+byLanguage、live+ops 四写,PHP 动态注入,**systemPrompt 零字数**。详见 [[project-thinknova-0729-screenwriter-stack]]。
- 🔴🔴 **4000 字紧缩令(07-31 老板通则)**:一切提示词≤4000字(字数),言简意赅目的明确;systemPrompt **现值现拉线上**(旧记 ~3460 已作废),单一口径、矛盾条款清零(旧11095字46段版存档于 `00_规格与参考/ARCHIVE_screenwriter_prompts_0731.json`)。**加任何新规则前先想清挤掉谁、与谁冲突。**
- **英文**严禁删空格拆词(words 按单词计,空格保留)。
- **台词**:句数跟 `shotCount`;连贯一段话拆开说(禁同构排比、禁句句名词开头、至少一句问句或感叹);复读=废片;感官词全片只用一次;至少一半句子说具体事实、空词最多 1 次;**商品名一字不差**(含商品名句放宽 16 字/12 词);`lines`=恰好 N 条完整句、句句标点收尾,禁半句禁合并禁说明文字。
- **画面**:台词画面同频;三要素(商品本体在场+进行中动作+微距点名部位);板片同源(boardCells 与 cells 同序号);**【产品形态交给参考图·硬规】禁编剧写形态词**(它看不到参考图,写"整只窑鸡"生图就画整只);行业模板仅供风格,品类不符禁灌行业典型物。
- **用户补充要求最高优先**且**必须整整一句专门表达**;`priorityOrder=[user_extra_requirement, selected_option]`。🔴 它能直接指挥画面(写"开场先给门店招牌全景"→ boardCells 格1 照做),但只能控"拍什么",**锁不住"是哪一家"**(靠参考图进 i2v)。
- **🔴 到店标准(07-20 拍板)**:提示词对"到店"必须**沉默**(不教不禁不提);偶发一句=正常,**每条必现=提示词在灌=错**。禁词自检是**临时手段**,灌输源断净后撤掉恢复沉默态。
- **CTA 空=零邀请**,邀请只许最后一句。**译名例外**:copyLanguage 与商品名语言不同用译名。
- ⚠️ 口播没声音先看 `fallbackReason: Operation timed out after 180001ms` + `source: local_fallback` = 编剧超时走了兜底模板(`fallbackPolicy=ambient_only`),**不是预设问题,重烧即可**。
- **编剧模型 = gpt-5.6-luna(461)现役**(07-19)。**别用歧义动词写模型指令**(曾写"数字数写进 lines"→ 字数进了台词、lines 乱码)。

## 五、视觉纪律(八类:色彩/肤质/灯光/人物/声线/镜头/物性/格位配额)
**完整措辞在 config,逐条备查见归档「视觉纪律全文」。** 只在这里记四条最容易被改错的:
- **全板人物锁**:每个出现服务者的格都是首帧那位,**无论是否上传人物照都生效**。
- **声线不锁死性别**,随出镜人物走。
- **走入式方向写死**:机位在店外朝店内推进,**禁从门内向外走**。
- 🔴 **负面词只禁"新增字幕水印",不泛化禁"文字"**——泛化会把真人真店一起净化掉。

## 六、字节纪律
- **i2v 提交串(videoPrompt)有两道墙**:🔴 **派发层 4000 字节自截(先撞上,丢的是台词)**;grok 上游 **4096 字节硬拒**(整单 400)。⛔「4096 已作废」这说法本身是错的——作废的只是「4096 适用于所有模型」。🔴 07-31 起语义变了:**超限=编剧该次尝试被杀重试**(scriptwriter videoPrompt exceeds 4096 bytes),不再静默截断。videoTemplate ⛔**08-11 更正:线上真值 363 字**(旧记 564字/1501B 作废);再往 videoTemplate 加字前仍要量。🔴 **别重写它的开场句**——08-11 有 agent 把有效的开场句"改写升级"成大段描述板子(拍摄计划表/格线/白缝/缩小复本),反而把模型注意力引到格子上,格网复发;已恢复原文。
- **超限后果以技术文档《07-12》为准**:裁静态/修饰前缀,**优先保台词+分镜 guard+参考图**。
  ❌「超 4096 = 静默砍尾但任务仍 succeeded」已推翻(07-24)。遇镜头/收尾丢失**带任务号找技术**。
- **「4096 超限」与「编剧回退」是两条独立机制**,别绑成因果(反证 task_afe9a1492361:i2v 3307B 正常出 5 句、编剧无回退)。

## 七、grok / i2v 能力边界(写了也不执行的别写)
> **优先级铁律(07-20 定调)**:先保出片率(≥90%)再谈效果。**grok 做不到的规则纯占字节,一律不写。**
- **grok 不能硬切**:一镜一换/景别跨档/角度30度/不越轴/cut on action/no crossfade 实拍证明全不执行,已从 videoTemplate 砍掉;它的切换是**叠化**=能力边界。快促片型也**别写格内硬切**。
- **单参模型只锁首帧**:只收裁格首帧 1 张,不吃 `reference_image_urls`。锁人物→首帧必须有该真人正脸,产品/门店同理。**裁的是格2(上中)=三者融合最全的锚格,t=0 像素锚 = 唯一锁真人脸机制**。张数见 [[reference-thinknova-multiref-model]]。
- **i2v 方差**:同板时而跟板时而塌成口播人头(首帧是 talking-head 时最易);灯光吃自己风格;倾向早说完留静默尾巴;饱和度偏高、加戏多。**板↔片一致性约 83%**(07-28)。**别下"逐格逐秒复现"结论**——它锁顺序和内容,切点与时长自己重排(2.3s 就提前切过);验成片必须 **1fps 抽帧 ≥15 帧通看**。
- **videoTemplate 保留项(别砍)**:单一全屏绝不分屏拼贴、人物以首帧为准(首帧无人则全片不添人)、台词每句只说一次+不加速吞字+数字用汉字读、同店同光同人+肤质真实+原相机直出+零文字水印。
- ⚠️ 根治板→片一致性 = **架构活**(分段生成再拼接),非提示词能解。

## 八、时长与分格(现行形式契约)
- `{{durationSeconds}}` 已替代写死的"15秒";时长可配 **5~30 秒**,现役档 8/10/12/15。
- `scriptwriterPreset.shotCount` 案例级 **1-7**,`defaultShotCount=5`,优先级 **案例 > Agent > 默认**;**只控编剧时间轴,与分镜板解绑**。
- 左上格 = **开场格**,六格全部可用。❌「左上格=纯黑屏(铁律)」已删 → `deliveryPostProcess.entranceBlackOverlay` 接管开场黑场(07-27)。**别再加回来。**
- ❌「输入含六宫格分镜板、按 6 格顺序演」已推翻 → 现行=按时间顺序镜头清单,`storyboardGuard` 写事实版(07-19);整板/裁格开关见 [[project-thinknova-film-types]]。
- **视频语言选项 = zh_cn/en/ja**(grok 口播只念中英日),海报保留全语言;⚠️曾被技术部署退回,动前先回读。**分镜板必留**(客户生成前确认剧情=剧情预览,是产品功能)。

## 九、待核
- **人脸镜头配额**(五镜至少两格无人脸、有脸最多三格、绝不连续三格同一张脸近景正脸)——07-20 提出的压审核手段,自述"尚未干净验证",至今无后续。红线是组合不是敏感词,见 [[reference-grok-content-policy]]。
- 10 秒台词字数两个口径未统一(见第四节)。
