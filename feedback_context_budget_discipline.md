---
name: feedback-context-budget-discipline
description: "触发:要点浏览器/要Read图/要分片打印长文本之前 → 合并成一段JS只回一行摘要;判断上下文为何满一律读 usage 字段不看字符数"
metadata: 
  node_type: memory
  type: feedback
  originSessionId: f9888687-3c16-4546-9394-03122edbc103
  modified: 2026-07-28T19:16:40.319Z
---

# 上下文预算纪律(2026-07-28 实测定案)

## 实测账(两个大会话,逐条拆的,不是估算)
| | 会话A | 会话B |
|---|---|---|
| **图片** | 48.2MB / 319张 | 69.4MB / 493张 |
| 工具返回(文本) | 25.9MB | 57.7MB |
| 我的输出 | 13.7MB | 6.2MB |
| 老板输入 | 0.29MB | 0.34MB |
| **记忆文件** | 0.008MB | 0.008MB |

812 张图里,**517 张是 claude-in-chrome 点鼠标白回的截图**(点一次回一张,约1500 token),
只有 ~300 张是真证据图/联系表。**一个会话点 172 次 = 白烧 26 万 token。**

→ **记忆文件完全不是原因(占 0.006%)。别再往那个方向找。**

## 铁律
1. 🔴 **后台操作一律 `read_page` / `get_page_text` / `javascript_tool` 拿文字,禁止用点击工具导航。**
   只在"必须给老板看画面"时截 1 张。
2. 🔴 **证据图走联系表**:一条视频压成 1 张 tile 图再 Read,不读原始逐帧图(ffmpeg 命令与逐帧取证标准 → 见 [[feedback-evidence-standard]],本文不重复)。
3. 🔴 **打印前先探字段结构**(曾把 3 个 URL 字符串打成 555 行按字符编号的对象)。
4. 大段提示词只取关键节,不全文拉。

## 🔴 真实 token 曲线(2026-07-28 从 transcript 的 usage 字段直读,这才是准数)
拿一个"只干一件事(改 config + 验证)"的会话量的:
| 时刻 | 上下文 |
|---|---|
| 开场什么都没干 | **50,641** |
| 10 分钟后 | 80,239 |
| 27 分钟后 | **177,714 → 顶满,自动压缩** |
| 压缩后重新开始 | 68,517(**压缩摘要本身就占 32,496**) |
| 再过 12 分钟 | 118,373 |

**关键**:那 45 分钟里发了 **211 次模型调用**,绝大多数每次只 +600~2,500 token,**没有单一元凶,是被 211 次小口啃满的**。
50,641 → 177,714 = 净增 12.7 万 ÷ 211 ≈ **每次调用 +600**。
→ 老板看到的"2 轮对话就满了",实际是**一轮 = 100+ 次工具调用 = 45 分钟连续作业**。

**必须接受的前提**:改线上 config 并逐条验证,本来就是吃满一个窗口的活。**目标不是"不压缩",而是"压缩前把结论落进记忆文件"**——只要落了,压缩两次也一条不丢。

## 🔴 省窗口第一杠杆:合并往返(比省图片更有效)
现在的坏模式:GET → 打印看 → 改 → PUT → GET 回读 → 核对镜像层,**六次调用六坨返回,同一段文字进上下文三次**。
正确做法:
1. **一段 JS 里做完整流程,只回一行 diff 摘要**(旧值→新值 + sha 变化 + 残留计数),六次变一次。
2. **返回值只回差异,不回全文**。config 1.9MB,禁止整段打印。
3. 需要确认的用**计数**代替原文(如「"文字清晰可读"残留 0 处」),别把整段贴回来。

## 🔴🔴🔴 2026-07-29 02:2x 第二次实测 —— **再次证实"没有单一元凶",并撤回我当场的一个错结论**
老板问"为什么上下文满这么快",我先按 jsonl **字符数**拆,得出「84% 是 Read 读图片(68张×21.7万字符)」。
❌ **这个结论是错的,已当场撤回**:jsonl 里存的是 base64,**字符数 ≠ 模型看到的 token**。一张图进上下文只有 ~1,500 token。
✅ 改用 `usage` 字段(真值)复测同一个会话:

| 指标 | 值 |
|---|---|
| 模型调用次数 | **1,758 次** |
| 开场基线 | 50,641 |
| 峰值 | **353,897** |
| 单次增量**中位数** | **1,052** |
| 单次最大增量 | 279,197(压缩摘要注入,非日常) |

→ **结论与 07-28 那次完全一致:被 1,758 次调用一口一口啃满,中位数才 1,052。**
→ **杠杆只有一个:减少往返次数。** 本会话光 `javascript_tool` 就打了 **397 次**(平均返回仅 650 字)——
   这正是本文件第 50 行「合并往返」那条纪律,**我写了但没执行**。

### 硬规(下次直接照做)
1. 🔴 **一段 JS 干完一整套流程,只回一行摘要**。禁止"取数→打印→再取数→再打印"这种逐步试探。
2. 🔴 **探索性查询先在脑子里列全清单,一次问完**(如"这三个字段+两个镜像+长度"合成一次调用)。
3. 🔴 **大段提示词禁止分片打印全文**(本会话分 3 片打了 7,566 字的 systemPrompt);改成正则只取命中句 ±60 字。
4. 🔴 **诊断类的大范围搜索派 subagent**,只要结论不要过程。
5. 判断"上下文为什么满"**一律读 `usage` 字段**,不要拿 jsonl 字符数当依据(我已犯错一次)。

## 补正:图片成本按尺寸算,不按文件大小
一张 1.27MB 的图和一张 300KB 的图,进上下文都被压到长边 ~1568px,**成本都是 ~1,500 token 上下**。
所以"这张特别大所以特别贵"是错的;**贵的是张数**,不是单张体积。

## 开场 ~50K 基线的构成(估算,非实测)
- MEMORY.md 7,931 字 ≈ 5K token;plan 文件 ≈ 3K
- 其余 ~60K = 常驻工具说明书(~42个)+ 待加载工具名单(~100个)+ 各 MCP 服务器的"使用须知"(**无条件加载,用不用都在**)
- 待加载机制(ToolSearch)已经在跑,不是没做延迟加载
- 老板侧唯一杠杆:关掉没授权也没用过的 design/marketing/sales 三个插件包(23个服务器),约省 20K。
  **但这 20K 只有那 26 万的 1/13 —— 优先级是我改习惯,不是他改配置。**

**Why**:老板连续三次会话被顶满,一直怀疑是记忆文件,实测证明是我的操作习惯。
**How to apply**:每次要点浏览器之前先问"我能不能读文字拿到";每次要 Read 图之前先问"能不能压成一张联系表"。

关联:[[feedback-memory-keep-current]] [[feedback-evidence-standard]] [[project-thinknova-storyboard-test]]

## 2026-09-05 · 额度消耗过快的根因与硬规矩(老板令:处理额度问题)
**根因(本周实证)**:①子 agent 单个动辄 100+ 次工具调用、20-30 万 token(浏览器逐步操作+轮询等渲染),一天 10+ 个这种 agent 就把 session 额度打爆(09-03~05 五次 429);②并行 3-4 个重 agent;③把上下文超重的 agent 反复 resume;④agent 内部挂计时器等平台渲染,每次醒来都是一轮 token;⑤全部默认走 fable-5-1(限额最紧的模型)。
**硬规矩**:
1. **并发上限 2**:同时最多两个子 agent,其余排队;老板不在线时上限 1。
2. **模型分级**:机械活(配置 diff/PUT 回读、文件装配、ffmpeg、烧单轮询、上传发布、只读核查)一律 `model: sonnet`;只有判断密集型(方案裁定/竞品分析/提示词设计)才用默认模型;Opus 只在 fable 过载时顶替。
3. **单 agent 预算**:任务书写明「工具调用 ≤30 次、不等渲染——建单后立即返回任务号,渲染完由总指挥另起轻装 agent 收片」;禁止 agent 内 sleep/计时器轮询。
4. **不 resume 超重 agent**:中断=写状态到磁盘,新起轻装 agent 从磁盘接;resume 只用于上下文小的 agent。
5. **一段 JS 干完一整套**(已有规矩)+ 报告只回摘要,不回全文。
6. **各线同规**:营销/邮件线同账号共享额度,章程同步这五条。
