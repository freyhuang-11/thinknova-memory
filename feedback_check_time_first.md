---
name: feedback-check-time-first
description: "触发:要说\"现在/刚才/多久之前\"、或要判断某单跑了多久之前 → 先跑 date 取真实时刻,不许凭感觉猜;写记忆用绝对时间戳"
metadata:
  type: feedback
---

系统提示只提供**日期**,不提供**时刻**。所以我天然不知道现在几点,也感知不到对话之间过了多久。

**不许凭感觉说时间。** 需要时刻时执行:

```bash
date "+%Y-%m-%d %H:%M:%S %A"
```

**Why**:2026-07-22 03:08,用户指出"你分不清楚时间,我每次和你聊都是过时间的"。此前我在回复里写"现在是凌晨3点多"——那是猜的,恰好蒙对。更实际的危害:核验任务时靠 `created_at` 判断哪单是刚烧的、一个 processing 的单是刚提交还是卡住了,不知道当前时刻就只能盲猜。

**How to apply**:
1. 判断"刚才/现在/多久之前"之前,先 `date`;
2. 核验烧单时,用当前时间和任务 `created_at` 相减,才能说清"这单跑了多久""这是不是最新一单";
3. 跨天工作时(用户常做到凌晨),日期会翻篇,别默认还是昨天;
4. 记录事故/结论进 vault 或记忆时,时间戳用 `date` 取,不要写"今晚""刚才"这类相对说法——[[feedback-memory-keep-current]] 要求记当前状态,相对时间过几天就失效了。


## 🔴 本机时区就是新加坡时间(+0800),别再推,直接跑 `date`

老板 08-13:「**时间统一按照新加坡时间计算**」。
本机 `date +%z` = **+0800**,即本机本地时间 **就是** 新加坡时间,不需要换算、不需要 `TZ=`。
(顺带:Git Bash 里 `TZ=Asia/Singapore date` 会因为缺 tzdata 而回落成 GMT,**别用它验证**,
 直接 `date` + `date +%z` 才是真值。)

## 🔴 08-13 又犯一次:拿文件 mtime 倒推日期

我看到刚渲的文件是 `08-12 03:13`,就对老板说"今天是 8 月 12 日";
实际 `date` = **08-13 04:17**。差一天,而且**直接影响他今天该发哪张微信卡**。

**根因**:会话跨了很长的真实时间(中间进程退出过),我拿会话里早先的时间感 + 文件 mtime 当"现在"。
**mtime 是"那个文件什么时候写的",不是"现在几点"。**

🔴 **规矩**:任何一句带"今天/昨天/几号/星期几/多久前"的话出口之前,**当场跑一次 `date`**。
不许用:文件 mtime、会话里早先跑过的 date、git log 时间、记忆里的印象。
这条 08-11 已经犯过一次(照抄旧文档把整周星期排早一天),08-13 换了个形式再犯。
